#include <iostream>
#include <algorithm>
using namespace std;
int a, b, c, d;
int main(){
	cin.tie(0); cout.tie(0);
	ios::sync_with_stdio(0);
	cin >> a >> b >> c >> d;
	cout << a * b + c * d << endl;
}